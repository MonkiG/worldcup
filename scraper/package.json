{
  "name": "worldcup-scraper",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "scrape": "npm run scrape:groups",
    "scrape:groups": "node scrape.mjs --target groups",
    "scrape:fixtures": "node scrape.mjs --target fixtures",
    "scrape:all": "node scrape.mjs --target all",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "commander": "^15.0.0",
    "playwright-core": "^1.53.1"
  }
}
